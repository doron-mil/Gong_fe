import moment from 'moment';
import {UpdateStatusEnum} from './updateStatusEnum';

export class ScheduledCourseGong {
  courseId: number;
  dayNumber: number;
  time: number;


  constructor() {
  }
}
